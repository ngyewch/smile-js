import t from 'tap';
import {InputStream} from '../src/inputStream.js';
import {BigDecimal} from '../src/bigDecimal.js';

t.test('should decode BigDecimal values', t => {
    // TODO

    t.end();
});
